const Error = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1 style={{ fontSize: 128, color: "purple" }}>Error</h1>
    </div>
  );
};

export default Error;
